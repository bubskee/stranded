package main

func main() {
	// TODO: flags — --id, --peers (comma-separated id=addr pairs), --data-dir
	// TODO: build node.Config, node.New(cfg)
	// TODO: register grpcServer on a grpc.Server, listen, go serve
	// TODO: n.Run(ctx) on main goroutine, handle SIGINT/SIGTERM for graceful shutdown
}
